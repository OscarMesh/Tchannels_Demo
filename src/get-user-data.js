export function getUserData(email, accountName) {
    // Use the email and accountName as needed
    console.log(`Received email: ${email}`);
    console.log(`Received accountName: ${accountName}`);
}